services       reproduced (4 reproduced)
  web          reproduced   web at http://127.0.0.1:8080, 3 of 3 instances
  worker       reproduced   worker, running, 2 of 2 instances
  events       reproduced   worker, running
  cache        reproduced   worker, running

database       reproduced (2 reproduced)
  data         reproduced   12 tables over 184000 rows, branched from gv_20260830120000_abcd1234
  provenance   reproduced   golden gv_20260830120000_abcd1234, 41 columns read back over 82000 rows sampled

third_party    not measured: the manifest names no third party hosts, and everything else is in block mode

auth           reproduced (1 reproduced)
  buyer        reproduced   signs in with password, in public.users

runtime        not measured: the environment runs on the local runtime, which publishes an address this machine can reach, and nothing in the manifest says what production runs on, so there is nothing to compare it against

traffic        not measured: the manifest does not ask for traffic, so there is none to reproduce

datastores     absent (1 absent, 1 unmeasured)
  cache        unmeasured   a redis declared empty, because a cache is rebuilt from the primary and a copy would be noise, and nothing here started it, so the declaration is recorded and unchecked
  events       absent       a clickhouse declared golden, and nothing here built one: no golden, no attestation, no tables and no rows. There is one golden, one masking pass, one verification scan and one branch, and all four are the primary Postgres

topology       reproduced (2 reproduced, 2 unmeasured)
  web          reproduced   3 of 3 instances
  worker       reproduced   2 of 2 instances
  events       unmeasured   this service names no count, so it runs one and nothing says whether production runs one
  cache        unmeasured   this service names no count, so it runs one and nothing says whether production runs one

9 of 10 measured components are production's own, which is 90 percent. 6 components and dimensions are excluded and named below.
A component is production's own when the environment reaches the real thing.
A substitution, a refusal and an absence are all counted and none of them
counts as reproduced. Nothing unmeasured is counted either way.

Not measured, and so not counted:
  datastores     cache: a redis declared empty, because a cache is rebuilt from the primary and a copy would be noise, and nothing here started it, so the declaration is recorded and unchecked
  runtime        the environment runs on the local runtime, which publishes an address this machine can reach, and nothing in the manifest says what production runs on, so there is nothing to compare it against
  third_party    the manifest names no third party hosts, and everything else is in block mode
  topology       cache: this service names no count, so it runs one and nothing says whether production runs one
  topology       events: this service names no count, so it runs one and nothing says whether production runs one
  traffic        the manifest does not ask for traffic, so there is none to reproduce
