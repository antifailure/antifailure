# added

The last three cells of the ordering table: an organization that exists before
its first event against one that does not, a clock two days wrong in either
direction at the site beacon, and the savepoint that stops a failed analytics
insert from poisoning the transaction it was called inside.
