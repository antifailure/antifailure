# added

The beacon does not count crawlers that run JavaScript, or driven browsers, and
an opt out now lasts longer than the tab. Opening any page with
`?af-analytics=off` switches measurement off for that browser, which is how
somebody who works here keeps their own reading out of the numbers.
