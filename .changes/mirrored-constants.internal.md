# added

Two constants whose comments claimed they matched the other side of the Go and
TypeScript boundary now have a test that reads the other side and says so:
`GrantableScopes` against the control plane's `GRANTABLE_SCOPES` and the sentence
in the signing-in guide, and `MaxBatch` against `MAX_BATCH`.
