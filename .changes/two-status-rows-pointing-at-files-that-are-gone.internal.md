# fixed

Two status rows carried evidence nobody could follow.

The audit that turned 13.12 from `written` into `proven` was pointed at the rest
of the page afterwards, and two rows had drifted from the rule that page sets
for itself.

`11.8 The site's API` named `.github/workflows/waitlist.yml` and
`www/lib/waitlist.ts` in the present tense and neither exists. The waitlist was
removed and its daily external probe went with it; `signup.yml` replaced it,
says so in its own header, and holds the same 07:00 UTC slot. A row whose
re-runnable evidence points at a file that is not there is the same defect 13.12
had, one level down. The history in the row is kept, because it is the best
account in this repository of a two day outage that CI, the deploy and every
file in the tree were green throughout. Only the tense and the filenames change.

`Load testing the control plane` quotes measured numbers from one run of
`engine/cmd/loadcp` on somebody's machine. It already said the numbers are not a
capacity figure, which is honest about the numbers and silent about the run.
`loadcp` appears in no workflow and in no justfile recipe, and its directory
holds `main.go` and `profile.json` and no test file at all, so nothing repeats
the run and nothing tests the command that produced it. The row now says that,
and says what it would take for CI to repeat it. It is not blocked on an account
like the provider rows are; it is only unbuilt.

Neither row is repaired by scheduling the thing it describes. A row that states
its own condition is worth more than one that quietly drops the claim.
