# fixed

The footer asked every visitor to request access, on every page of the site.

Signups have been open since the GitHub App landed: the signup page itself
says "One button, no card, and no invitation needed". The footer's two links
to it still read "Request access", which is the wording from when the hosted
control plane was behind an invitation wall, and the footer is on every page,
so that was the last sentence a reader saw before deciding whether they could
try it. Both links now say Start free, which is what the button on the page
they lead to does.
