# fixed

The transforms reference prose gate reads the half a generator does not own. The
sentence naming which transforms preserve uniqueness became generated, so the
gate now checks the hand written half that names the ones that do not, and its
emptiness guard fires on that half instead.
