# fixed

`just tagsync` now holds the four version literals in the release verification
page to the release being cut, so the worked example cannot go on naming a tag
whose assets do not match the instructions.
