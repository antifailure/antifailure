# fixed

- The console could show a workflow as passed after the engine had marked it
  unverified for using a response invented by a model. Workflow verdicts and run
  totals are now sent after the engine finishes its checks.
