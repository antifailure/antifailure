# fixed

The verdict card on the home page no longer scrolls its log rows forever, and
`just motioncheck` refuses any page whose built stylesheet or HTML animates
without stopping, including `animate-pulse` and `animate-ping`.
