# fixed

`af explain` no longer runs past the right margin. A migrate command, a seed
command, a rule's note, an invariant's description and the resolved secret
sources all wrap under their own column, and the whole page fits at 40, 80,
120 and 200 columns.

A support bundle renders at the default width whatever terminal produced it,
so two bundles of the same manifest are the same file.
