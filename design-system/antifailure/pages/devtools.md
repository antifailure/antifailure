# Devtools — overrides

Master: Brutalism, variance 8, motion 8. Developer-tools solutions vertical. Matches product chrome.

- Hero: asymmetric — copy left, product `MigrationFilm` with `tab={1}` (expand-and-contract) right.
- Drop homepage `MigrationScene` and `LockChartMobile`.
- Plan regression is a framed mono slab, 12px / 2px.
- Colors: neon primary `#33BF00`, sage wash `#E4F1EB`, CTA `#EA580C` on black text.
- Radii: frames 12px, stamps/CTAs 8px. Charts: film bars only. No GSAP.
