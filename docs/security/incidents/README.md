# Security incidents

One file per incident, named `YYYY-MM-DD-short-slug.md`, recording what
happened, when it was detected and by what, what data was reachable, what was
rotated, and what control was added so that the same thing cannot recur
silently.

An empty directory means there has not been one. It is deliberately not
deleted, so that the absence is visible rather than ambiguous.
