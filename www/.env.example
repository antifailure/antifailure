# Auth.js. Generate one with: npx auth secret
AUTH_SECRET=
AUTH_URL=http://127.0.0.1:3000

# GitHub → Settings → Developer settings → OAuth Apps
# Callback: {AUTH_URL}/api/auth/callback/github
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# Google Cloud Console → APIs & Services → Credentials → OAuth client (Web)
# Callback: {AUTH_URL}/api/auth/callback/google
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Azure Portal → App registrations
# Callback: {AUTH_URL}/api/auth/callback/microsoft-entra-id
AUTH_MICROSOFT_ENTRA_ID_ID=
AUTH_MICROSOFT_ENTRA_ID_SECRET=
AUTH_MICROSOFT_ENTRA_ID_ISSUER=https://login.microsoftonline.com/common/v2.0

# PostHog, for product analytics and session replay on the marketing site.
#
# NEXT_PUBLIC_POSTHOG_KEY is a PostHog "project API key". It is public by
# design: it ships inside browser JavaScript, it can only write events into one
# project, and it reads nothing back. It is not a secret and it does not belong
# in a vault. A fork sets its own here; the empty string turns PostHog off for
# a build, including the request that would have fetched the library.
NEXT_PUBLIC_POSTHOG_KEY=phc_BXsb8vQVdiajf7uG9soRdsEwLtcE7tJWgdAfc4Vvoqau

# Where the browser sends events, AND where it fetches the session replay
# recorder from. One base for both: posthog-js treats a custom host as region
# "custom" and routes capture, feature flags, remote config and the script
# bundles all at it. There is deliberately no separate asset host variable,
# because setting one sends the bundles to a posthog.com address and undoes the
# arrangement.
#
# This is an endpoint this project runs on its own domain, not a posthog.com
# host, and not this site's own origin: the site is a static export with no
# server, so the proxy lives on the control plane. Same site, different origin.
# It is TRANSPORT and not a boundary. PostHog receives the data either way.
NEXT_PUBLIC_POSTHOG_API_HOST=https://app.antifailure.dev/ph

# Where a link built by the library should point. Never the proxy: the proxy
# serves the ingest API and knows nothing about the PostHog application. This
# is the only posthog.com host that belongs in this tree, because it is a link
# a person clicks and the browser never fetches it.
NEXT_PUBLIC_POSTHOG_UI_HOST=https://us.posthog.com
